============
Installation
============

At the command line::

    $ easy_install django-coin-hive

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-coin-hive
    $ pip install django-coin-hive
